# 1) create venv and install
python3 -m venv venv
source venv/bin/activate          # (mac/linux). On Windows use the PowerShell command above.
pip install --upgrade pip
pip install -r requirements.txt

# 2) run pipeline
python3 generate_data.py
python3 preprocess.py
# try MILP (requires pulp)
python3 model_core.py
# fallback (fast)
python3 greedy.py
# Validate outputs
python3 validate.py

# check results
ls results
